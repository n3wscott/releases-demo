name: Release Pipeline

on:
  push:
    branches:
      - release-*

jobs:
  semver:
    name: semver
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go ${{ matrix.go-version }}
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go-version }}

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: GO111MODULE=on go get github.com/n3wscott/releases-demo/semver@main

      - name: Look at Ref
        run: |
          semver $GITHUB_SERVER_URL/${{ github.repository }}.git ${{ github.ref }}
